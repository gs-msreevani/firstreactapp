import { useTranscoders } from "./transcoder";
import type { TranscoderOptions } from "./transcoder";
import type { History } from "history";
export declare type HashOptions = TranscoderOptions & {
    window?: Window;
};
declare const useHashHistory: ({ window, ...config }?: HashOptions) => History;
export { useTranscoders, useHashHistory };
